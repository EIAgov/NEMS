* D2.INC...INCLUDE FOR DISTRIB.RUL
* ...DOES NOT USE BLOCKING

$ANALYZE SUB COL H X=.1/*
$ANALYZE SUB ROW D
$ANALYZE SUB SET

$SET AREA=
 The harvesting is distributed over the %NROWS time periods as follows
$LOOP
$SET AR %COLUMN(2:3)
$SET T  %COLUMN(4:4)
$IF @%%AR <> @%%AREA THEN GOTO NEWAREA
, %%T
$SKIP LOOP
:NEWAREA
.
$TEXT
 Area
$ENTITY %%AR AR
is harvested in %%T
$SET AREA=%%AR
$NEXT COL
.

$GOTO EXIT

* SETBASIS.RUL
* INCLUDE FILE TO SETUP BASIS FOR RULE FILE
$IF %SWRATE = T THEN GOTO RATESET
 Setting up basis...please wait.
$SET SWMSG = %SWMSG
$ANALYZE SW MSG=F
$ANALYZE BASIS
$ANALYZE SW MSG=%%SWMSG
$IF %SWRATE = T THEN GOTO RATESET
 Sorry, cannot setup basis...must abort.
$EXIT
:RATESET
